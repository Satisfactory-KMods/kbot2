import { MO_ReactionRoles } from '@shared/types/MongoDB';
import { FC } from 'react';

interface ReactionRoleRowProps {
	reactionRole: MO_ReactionRoles;
}

const ReactionRoleRow: FC<ReactionRoleRowProps> = ({ reactionRole }) => {
	return <></>;
};

export default ReactionRoleRow;
